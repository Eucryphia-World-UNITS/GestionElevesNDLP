{% extends 'base.html.twig' %}

{% block title %}CourOption{% endblock %}

{% block body %}
    <h1>CourOption</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cour_option.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ cour_option.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cour_option_index') }}">back to list</a>

    <a href="{{ path('cour_option_edit', {'id': cour_option.id}) }}">edit</a>

    {{ include('cour_option/_delete_form.html.twig') }}
{% endblock %}
