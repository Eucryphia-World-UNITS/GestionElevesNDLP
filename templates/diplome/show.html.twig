{% extends 'base.html.twig' %}

{% block title %}Diplome{% endblock %}

{% block body %}
    <h1>Diplome</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ diplome.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ diplome.name }}</td>
            </tr>
            <tr>
                <th>Lv2_obligatoire</th>
                <td>{{ diplome.lv2Obligatoire ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('diplome_index') }}">back to list</a>

    <a href="{{ path('diplome_edit', {'id': diplome.id}) }}">edit</a>

    {{ include('diplome/_delete_form.html.twig') }}
{% endblock %}
