{% extends 'base.html.twig' %}

{% block title %}EnseignementComp index{% endblock %}

{% block body %}
    <h1>EnseignementComp index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for enseignement_comp in enseignement_comps %}
            <tr>
                <td>{{ enseignement_comp.id }}</td>
                <td>{{ enseignement_comp.name }}</td>
                <td>
                    <a href="{{ path('enseignement_comp_show', {'id': enseignement_comp.id}) }}">show</a>
                    <a href="{{ path('enseignement_comp_edit', {'id': enseignement_comp.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('enseignement_comp_new') }}">Create new</a>
{% endblock %}
