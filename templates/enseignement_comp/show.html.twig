{% extends 'base.html.twig' %}

{% block title %}EnseignementComp{% endblock %}

{% block body %}
    <h1>EnseignementComp</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ enseignement_comp.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ enseignement_comp.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('enseignement_comp_index') }}">back to list</a>

    <a href="{{ path('enseignement_comp_edit', {'id': enseignement_comp.id}) }}">edit</a>

    {{ include('enseignement_comp/_delete_form.html.twig') }}
{% endblock %}
