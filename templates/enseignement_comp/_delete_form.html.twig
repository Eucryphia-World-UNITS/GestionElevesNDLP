<form method="post" action="{{ path('enseignement_comp_delete', {'id': enseignement_comp.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ enseignement_comp.id) }}">
    <button class="btn">Delete</button>
</form>
